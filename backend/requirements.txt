# Dependencias de ejecución del backend (Python 3.11/3.12).
flask>=3.0
flask-cors>=4.0
psycopg[binary,pool]>=3.1
python-dotenv>=1.0
requests>=2.31

# Pipeline de IA (tesis: OpenCV + MediaPipe Hands + TensorFlow/Keras).
# Son pesadas pero las exige /api/reconocer; sin ellas el endpoint
# responde 503 y el resto del diccionario funciona igual.
numpy>=1.26
opencv-python>=4.9
mediapipe>=0.10.14
tensorflow>=2.16
