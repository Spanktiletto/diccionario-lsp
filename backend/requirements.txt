# Dependencias de ejecución del backend (Python 3.11/3.12).
# Las dependencias del pipeline de IA (tensorflow, mediapipe, opencv)
# se añaden en la etapa de IA para mantener ligero el entorno.
flask>=3.0
flask-cors>=4.0
psycopg[binary,pool]>=3.1
python-dotenv>=1.0
requests>=2.31
